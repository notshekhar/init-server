This is a node CLI, I've createdto Initialize wih basic express app
To create basic express app, Run:

-   `npm i -g init-express`
-   `init-express <project-name>`
-   `cd <project-name>`
-   `npm i`
